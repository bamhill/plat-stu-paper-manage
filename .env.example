# Docker 对外端口；默认 3000
STUMANAGE_PORT=3000

# 建议正式部署时设置为至少 32 字符的随机字符串。
# 留空时，系统会在 data/auth-secret 首次生成并持久化。
AUTH_SECRET=

# 可选：首次认领包内已有数据时使用。
# 留空时系统首次启动会自动生成认领码，并写入启动日志与 data/legacy-claim-code。
LEGACY_CLAIM_CODE=

# 本地 npm 运行时使用；Docker Compose 已固定为同一数据库位置。
DATABASE_URL="file:./data/dev.db"
